# Gesture Particle Flow | 手勢流動 🌊

**Gesture Particle Flow** 是一個基於 WebGL 的即時 3D 粒子互動實驗室。它結合了 **Three.js** 的視覺渲染能力與 **MediaPipe** 的電腦視覺技術，讓使用者能透過網路攝影機（Webcam），以「手勢」隔空控制粒子的流動、形狀變換與物理效果。

![Project Banner](https://images.unsplash.com/photo-1550751827-4bd374c3f58b?q=80&w=2070&auto=format&fit=crop) 
*(示意圖)*

## ✨ 特色功能 (Features)

*   **🖐️ AI 手勢識別**：使用 MediaPipe 實時偵測手部關鍵點，無需特殊硬體，僅需 Webcam。
*   **💠 粒子變形 (Morphing)**：在不同 3D 形狀間平滑轉換（愛心、花朵、土星、煙火）。
*   **📹 掃描模式 (Scanner Mode)**：將攝影機影像即時轉換為 3D 粒子浮雕，並根據亮度產生深度。
*   **🎨 高度客製化**：可調整粒子數量（最高支援 30,000+）、顏色、噪聲強度與大小。
*   **🌍 多語言支援**：內建繁體中文、簡體中文、英文、日文、韓文。
*   **⚡ 效能優化**：提供「高畫質」與「省電/流暢」模式，適應不同裝置效能。

## 🎮 操作指南 (How to Use)

1.  **開啟相機**：點擊右上角的相機圖示 📷，並允許瀏覽器存取權限。
2.  **手勢控制**：
    *   **旋轉 (Rotate)**：傾斜手掌（像轉方向盤一樣）來旋轉 3D 物體。
    *   **縮放 (Pinch)**：捏合拇指與食指可將粒子向中心壓縮；張開雙手可控制擴散範圍。
    *   **打散 (Scatter)**：快速揮動手掌（Wave），產生氣流將粒子打散。
    *   **衝擊 (Hole/Flick)**：快速彈指或手指戳刺，在粒子群中產生穿透的衝擊波。
3.  **設定面板**：點擊右下角的齒輪圖示 ⚙️ 可開啟控制面板，切換形狀或調整參數。

> **注意**：請在光線充足的環境下使用，以獲得最佳的手勢識別效果。

## 🛠️ 技術棧 (Tech Stack)

*   **Frontend**: React, TypeScript, Vite
*   **3D / WebGL**: Three.js, @react-three/fiber, @react-three/drei
*   **Computer Vision**: Google MediaPipe Tasks Vision
*   **State Management**: Zustand
*   **Styling**: TailwindCSS
*   **Deployment**: Docker, Nginx

## 🚀 本地開發 (Local Development)

若您想在本地端修改程式碼：

1.  **安裝依賴**：
    ```bash
    npm install
    ```

2.  **啟動開發伺服器**：
    ```bash
    npm run dev
    ```
    打開瀏覽器前往 `http://localhost:5173`。

3.  **建置專案**：
    ```bash
    npm run build
    ```

## 🐳 Docker 部署教學 (Deployment)

本專案已包含 `Dockerfile` 與 `nginx.conf`，可輕鬆打包為容器映像檔。

### 1. 建置映像檔 (Build Image)

在專案根目錄執行以下指令：

```bash
docker build -t gesture-flow .
```

### 2. 啟動容器 (Run Container)

將容器的 80 Port 對應到本機的 8080 Port：

```bash
docker run -d -p 8080:80 --name my-gesture-app gesture-flow
```

### 3. 瀏覽應用程式

打開瀏覽器，前往：
`http://localhost:8080`

### 4. 停止與移除

```bash
docker stop my-gesture-app
docker rm my-gesture-app
```

---

## 📂 專案結構

```
.
├── src/
│   ├── components/    # React 元件 (Particles, UI)
│   ├── services/      # MediaPipe 視覺辨識邏輯
│   ├── utils/         # 數學運算與圖形生成
│   ├── store.ts       # Zustand 全域狀態管理
│   └── App.tsx        # 主程式入口
├── Dockerfile         # Docker 建置腳本
├── nginx.conf         # Nginx 伺服器設定
└── README.md          # 專案說明
```

## 📜 License

MIT License
